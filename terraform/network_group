resource "yandex_lb_target_group" "redmine network group" {
  name      = var.target_group_name
  target {
    subnet_id = var.subnet_a_id
    address   = var.subnet_a_ip_web1
  }
  target {
    subnet_id = var.subnet_b_id
    address   = var.subnet_b_ip_web1
  }
}